import 'package:flutter/material.dart';

class PredictionsSreen extends StatelessWidget {
  const PredictionsSreen({super.key});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text('Predictions page!'),
    );
  }
}
